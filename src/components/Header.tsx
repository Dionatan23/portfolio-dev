export function Header() {
  return (
    <header className="flex items-center justify-center w-full">
      <nav className="flex items-center mt-10 gap-24 h-12 ">
        <a href="/">Home</a>
        <a href="/">About me</a>
        <a href="/">Work</a>
        <a href="/">Contacts</a>
      </nav>
    </header>
  );
}
